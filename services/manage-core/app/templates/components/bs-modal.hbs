{{#if show}}
  {{#ember-wormhole to=to}}
    {{#bs-modal/wrapper
      fade=fade
      backdrop=backdrop
      keyboard=keyboard
      focus=focus
      show=show
      size=size
      contentClass=contentClass
      dialogClass=dialogClass
      onHide=onHide
      onHidden=onHidden
      onClose=onClose
      onShow=onShow
      onShown=onShown
    as |wrapper|
    }}
      {{yield wrapper}}
    {{/bs-modal/wrapper}}
  {{/ember-wormhole}}
{{/if}}
