import { getAsObject } from '@base-cms/object-path';

$ const content = getAsObject(input, 'content');

<document>
  <@head>
    <cms-page-metadata for="content" ...content />
    <${input.head} />
  </@head>
  <cms-page-container for="content" class="container-fluid-max" data=content>
    <cms-ad-placeholder size="970x90" class="mb-4 text-center" />
    <${input.renderBody} />
  </cms-page-container>
  <site-footer />
</document>
