import { get } from '@base-cms/object-path';

<website>
  <@head>
    <title>${get(input, "content.name")}</title>
    <cms-rel-canonical pathname=get(input, "content.canonicalPath") />
    <${input.head} />
  </@head>
  <site-navbar />
  <cms-container for="content" class="container-fluid" data=data.content>
    <${input.renderBody} />
  </cms-container>
  <hr>
  <site-footer />
</website>
