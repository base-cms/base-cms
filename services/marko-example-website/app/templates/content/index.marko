import { get } from '@base-cms/object-path';

<layout content=get(data, "content")>
  <h1>${get(data, "content.name")}</h1>
  <hr>
  <h4>$!{get(data, "content.teaser")}</h4>
  <hr>
  $!{get(data, "content.body")}
</layout>
