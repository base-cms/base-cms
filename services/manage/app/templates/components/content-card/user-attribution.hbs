{{#if show}}
  {{entypo-icon "clock" class="mr-1"}}
  <div>
    {{label}}
    {{#if fromNow}}
      <abbr title={{dateDisplay.title}} {{action "toggleDates"}}>{{dateDisplay.value}}</abbr>
    {{/if}}
    {{#if username}}
      by {{username}}
    {{/if}}
  </div>
{{/if}}
