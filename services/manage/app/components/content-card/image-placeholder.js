import Component from '@ember/component';

export default Component.extend({
  classNames: ['bg-gradient-primary', 'w-100', 'h-100', 'd-flex', 'align-items-center', 'justify-content-center'],
  icon: 'image',
});
