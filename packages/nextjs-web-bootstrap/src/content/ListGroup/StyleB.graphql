fragment ContentListGroupStyleB on Content {
  id
  type
  shortName
  published
  canonicalPath
  primarySection {
    id
    name
    alias
  }
  # @todo Remove the hardcoded CDN!
  primaryImage {
    id
    src(input: { host: "cdn.officer.com" })
    alt
  }
  company {
    id
    name
    canonicalPath
  }
}
