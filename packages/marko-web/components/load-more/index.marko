<!-- fragments must also be imported/registered and not be dependent -->

$ console.log("load-more input", input);

<wrapper|{ nodes }| ...input>
  $ console.log("nodes", nodes.length);
  <!-- @todo this must load a layout by name (via a registry?), otherwise it won't load -->
</wrapper>
