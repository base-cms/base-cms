$ const classNames = input.class;

<audio controls class=classNames>
  <source src=input.value type="audio/mpeg">
  <p>Your browser doesn't support HTML5 audio. Here is
    a <a href=input.value>link to the audio</a> instead.</p>
</audio>
