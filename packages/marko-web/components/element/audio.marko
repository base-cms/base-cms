<if(input.src)>
  <audio ...input.attrs src=input.src class=input.class controls="controls">
    <p>Your browser doesn't support HTML5 audio. Here is a <a href=input.src>link to the audio</a> instead.</p>
  </audio>
</if>
