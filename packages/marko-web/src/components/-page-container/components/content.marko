import { get } from '@base-cms/object-path';

$ const id = get(input, 'data.id');
$ const type = get(input, 'data.type');

<${input.tag || 'article'}
  data-id=`${id}`
  class=['content', 'content--display', `content--${type}`, input.class]
  ...input.attrs
>
  <${input.renderBody} />
</>
