import { get } from '@base-cms/object-path';

$ const id = get(input, 'data.id');
$ const type = get(input, 'data.type');
$ const block = 'content-page';


<${input.tag || 'article'}
  class=[block, `${block}--${type}`, `${block}--${id}`, input.class]
  ...input.attrs
>
  <${input.renderBody} />
</>
