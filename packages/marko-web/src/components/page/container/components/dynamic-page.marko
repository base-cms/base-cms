import { get } from '@base-cms/object-path';

$ const id = get(input, 'data.id');
$ const block = 'dynamic-page';

<${input.tag || 'main'}
  class=[block, `${block}--${id}`, input.class]
  ...input.attrs
>
  <${input.renderBody} />
</>
