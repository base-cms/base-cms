import { cleanPath } from '@base-cms/utils';

$ const href = `${out.global.requestOrigin}/${cleanPath(input.pathname)}`;

<if(input.pathname)>
  <if(input.renderBody)>
    <${input.renderBody} href=href />
  </if>
  <else>
    <link rel="canonical" href=href />
  </else>
</if>
