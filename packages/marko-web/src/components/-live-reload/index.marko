$ const { NODE_ENV, LIVERELOAD_PORT } = process.env;
$ const isDevelopment = NODE_ENV === 'development';

<if(isDevelopment && LIVERELOAD_PORT)>
  <script src=`http://0.0.0.0:${LIVERELOAD_PORT}/livereload.js` async></script>
</if>
