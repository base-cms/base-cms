import { get } from '@base-cms/object-path';

$ const lang = get(out.global, 'config.locale');

<!doctype html>
<html lang=lang dir=input.dir>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content=input.viewport>
    <${input.head} />
    <lasso-head />
  </head>
  <body>
    <${input.bodyHeader} />
    <${input.renderBody} />
    <${input.bodyFooter} />
  </body>
</html>
