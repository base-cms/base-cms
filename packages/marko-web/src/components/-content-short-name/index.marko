$ const attrs = {
  tag: input.tag,
  collapsible: input.collapsible,
  class: input.class,
  attrs: input.attrs,
  default: input.default,
  renderBody: input.renderBody,
};

<-value for="content" obj=input.obj path="shortName" ...attrs />
