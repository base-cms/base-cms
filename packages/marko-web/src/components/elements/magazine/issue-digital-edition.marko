import { get } from '@base-cms/object-path';

$ const props = {
  to: input.obj,
  path: 'digitalEditionUrl',
  ...input,
  attrs: {
    title: get(input.obj, 'name'),
    class: 'btn btn-sm btn-block btn-primary',
    ...input.attrs,
    },
};

<cms-link-element ...props />
