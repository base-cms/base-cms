$ const { config } = out.global;

<!doctype html>
<html lang=config.locale() dir=(input.dir || 'ltr')>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content=(input.viewport || 'width=device-width, initial-scale=1, shrink-to-fit=no')>
    <link rel="stylesheet" href="/dist/index.css">
    <!-- @todo determine if this should go at the bottom of the page -->
    <!-- @todo determine how to split assets out by vendor, core, site, etc. -->
    <assets />
    <${input.head} />
  </head>
  <body>
    <${input.bodyHeader} />
    <${input.renderBody} />
    <!-- @todo This should use internal JS -->
    <!-- Must always be included now since loadmore uses it (regardless if lazyload images are enabled) -->
    <script src="https://afarkas.github.io/lazysizes/lazysizes.min.js" async></script>
    <${input.bodyFooter} />
    <cms-document-live-reload />
  </body>
</html>
