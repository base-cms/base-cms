$ const attrs = {
  tag: input.tag || 'time',
  collapsible: input.collapsible,
  class: input.class,
  attrs: input.attrs,
  default: input.default,
  renderBody: input.renderBody,
  dateFormat: input.dateFormat || 'MMM Do, YYYY',
};

<-value for="content" obj=input.obj path="published" ...attrs />
