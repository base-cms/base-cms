import { asObject } from '@base-cms/utils';

$ const name = input.name || 'dataLayer';
$ const data = asObject(input.data);
$ const script = `${name}.push(${JSON.stringify(data)});`;

<script>${script}</script>
