$ const attrs = ["ln", "lc", "lcv", "lkw"].filter((k) => input[k]).map((k) => `${k}="${input[k].replace(/"/g, "&quot;")}"`);
$ const comments = `<!--DPM: ${attrs.join(' ')} -->`;

<if(attrs.length)>
  $!{`<!--DPM: ${attrs.join(' ')} -->`}
</if>
