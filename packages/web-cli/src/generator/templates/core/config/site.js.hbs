module.exports = {
  {{#if-rm siteLogo}}logo: '{{siteLogo}}',{{/if-rm}}
  {{#if-rm sections.length}}
  primaryNavItems: [
  {{#each sections as |section|}}
    { href: '/{{section.alias}}', label: '{{section.name}}' },
  {{/each}}
  ],
  secondaryNavItems: [
  {{#each sections as |section|}}
    { href: '/{{section.alias}}', label: '{{section.name}}' },
  {{/each}}
  ],
  {{/if-rm}}
};
