const each = require('./each');
const eachSeries = require('./each-series');
const series = require('./series');
const whilst = require('./whilst');

module.exports = {
  each,
  eachSeries,
  series,
  whilst,
};
