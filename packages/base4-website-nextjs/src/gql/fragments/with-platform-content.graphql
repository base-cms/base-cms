#import './content-canonical-path.graphql'

fragment WithPlatformContentFragment on PlatformContent {
  ...ContentCanonicalPath
  id
  name
  type
  teaser
  body
  redirectTo
  metadata {
    title
    description
  }
  primarySection {
    id
    name
    alias
  }
}
