fragment WithPlatformContentFragment on PlatformContent {
  id
  name
  type
  teaser
  body
  redirectTo
  canonicalPath(input: { fields: $canonicalFields })
  metadata {
    title
    description
  }
  primarySection {
    id
    name
    alias
  }
}
